# MTM Contract 2.0 — 把便宜的查證擺到昂貴的生成之前

**一套給「用 AI agent 做軟體產品的人」的排序紀律。**
少繞路、少重工、更多工作第一次就對，並且少花錢反覆重新推導你早就決定過的事。

---

**發表日期：** 2026 年 7 月 30 日
**作者：** 皓品智慧有限公司（Vast Intelligence Limited）
**名稱由來：** MTM = *machine to machine*（機器對機器）。契約是一種**交接格式** — 目前多半是人寫給 agent，設計上同樣適用於 agent 之間。編碼會換，欄位不換。
**規格版本：** 發表時為 2.0；現行 **2.4** — 新增的不變式見 §9（以及「拿來擋 finding 的決定，本身必須有依據」這條），另加了「症狀進來但 scope 未知」的 debug 分支、「gate 要硬就必須有東西真的在執行它」，以及「交代的東西是否真的通往他說的目的」這道檢查。一條 lifecycle、一個綠地分支、一具進化引擎。規格本體：[`MTM-CORE.zh-TW.md`](./MTM-CORE.zh-TW.md)（英文版：[`MTM-CORE.md`](./MTM-CORE.md)）
**Repository：** github.com/jewanchen/mtm-contract
**授權：** Apache 2.0（見 [`LICENSE`](./LICENSE)），衍生作品需署名（見 [`NOTICE`](./NOTICE)）
**English version:** [`mtm-contract-2.0-article.md`](./mtm-contract-2.0-article.md)
**前作：** [第一篇文章](./mtm-contract-technical-article.md)（2026 年 5 月）提出 11 欄契約。本文發表 2.0 架構與它背後的理由，不必先讀前作。

> **證據基礎。** 以下所有內容都是在出貨一個商業產品的過程中長出來的：行動端 App、後端服務、兩個 web 管理台，實作大部分由 AI agent 完成。11 週約一百份契約，外加一次在另一個綠地專案上的對照實驗。這些證據的界限寫在 §13，不埋在註腳裡。

---

## 0. 主張

如果你用 AI agent 做產品，限制你產能的不是 agent 寫得多快，而是**它多常寫錯東西** — 以及你有多少預算耗在發現寫錯、把它拆掉、重新建立你本來已經想清楚的東西上。

MTM Contract 是一套重新排序工作的紀律。一句話：

> **把便宜的查證擺到昂貴的生成之前。**

排序是脊椎，但它不是單獨運作的。另外兩個性質也承重，並在它們出現的地方各自點名：那份產物活在**檔案**裡而不是對話裡（§2），以及**被宣稱的檢查永遠不等於被執行的檢查**（§6.4）。四件事是這三者合起來的下游結果。每件都有對應機制，也都有一條「到這裡就不成立」的界線：

| 你得到什麼 | 產生它的機制 | 哪裡不成立 |
|---|---|---|
| **效率提升** | 歧義在還是一句話的時候被解決、不是在已經是一份 diff 之後。改方向發生在五分鐘的對話裡，不是在 rollback 裡。 | 小到「整個改動比它的描述還短」的任務。§5 的路由規則就是為了讓你不要在這裡付錢。 |
| **少繞路** | 動手前，agent 必須把它的假設逐條寫出來、並寫下每條是怎麼查的。舉不出查證的假設，就是你第一個要看的東西。 | 它會減少繞路、不會根除。§11 列出照樣漏掉的那些。 |
| **第一次就對的比例提高** | 每個聲稱的結果都必須綁到**實際觀察到的東西**、不是綁到承諾。並且由一個沒有寫過這段 code 的讀者，在它被算作完成之前對照計畫檢查一次。 | 那道審查是「乾淨的 context」、不是獨立的另一個團隊。§9 明講這條界線。 |
| **整體花費下降** | 貴的是走錯路，而安靜在燒的是反覆重新推導 context。一份短的書面產物是被**讀取**的；一段對話是被**重建**的。 | 我們量的是重試、重建、猜測輪數 — **不是 token 數**。全文不宣稱任何倍數。見 §13。 |

以下依序是：浪費到底發生在哪（§1）、排序上的修正（§2）、實作這個修正的架構（§3–§5）、為什麼長這樣（§6）、一個任務從頭走到尾（§7），然後是誠實的部分 — 它做不到什麼、代價是什麼、以及這些證據支持到哪裡為止。

---

## 1. 浪費到底發生在哪

第一次生成不是貴的地方。觀察一整天的 agent 協作開發，成本集中在五個地方，而沒有一個是「模型不夠聰明」。

**一、生成先於發現。** agent 開始寫實作，然後**在寫的過程中**才發現它正在處理的東西長什麼樣：某個欄位可以為空、某個 helper 早就存在、某個端點回傳的外層結構跟假設的不一樣。發現本身不是問題，**在生成之後才發現**才是 — 因為它讓已經存在的工作作廢。同一個查詢早六十秒做，成本是一次搜尋，而且不會動到任何別的東西。

**二、沒驗的前提會擴散。** 一個開頭沒查的假設不會只是一個錯。它會塑形資料模型，然後端點，然後前端，然後為了確認它而寫的測試。等它終於出事，代價不是那個假設本身，而是所有為了跟它一致而蓋出來的東西。第一步查很便宜，第十步拆很貴。

**三、重新推導。** 只活在對話裡的決定，你會付第二次錢。長時間工作之後、context 被摘要之後、或換一個 session 接手之後，agent 會靠重讀原始碼與重新推測意圖來還原「現在到哪了」 — 而且有時候會還原成**稍微不一樣的版本**，這正是第三步的決定為什麼會在第十五步被默默推翻。凡是你決定了卻沒寫下來的，你都會買兩次。

**四、猜測迴圈。** 症狀出現、agent 提出一個合理的假設、改 code、重建、再試一次。第二輪是從第一輪的殘渣上開始的，所以錯誤假設會累積、**後面的輪次比前面的更糟**。這是 agent 開發裡最貴的模式，也是最容易踩進去的，因為每一輪單獨看起來都像有進展。

這段期間的一個實例：某份成員名單在該顯示姓名的位置顯示了 email。沒有 crash、沒有 log。一輪接一輪完全合理的推論 — 是不是登入提供者不再回傳姓名、是不是前端不再送、是不是舊帳號需要回填 — 每輪都伴隨一次重建。終結它的是**一次跨服務的欄位搜尋**：一個共用 helper 帶著完整的 fallback 鏈，但另外好幾個地方各自 inline 拼出同一個字串、從來沒呼叫那個 helper。另外還有一個「第二個 bug」：一個該跳出來的關卡沒跳。它根本不存在 — 名單只是把要顯示的東西蓋掉了。

**五、宣告而未執行。** 這一項是能力變強之後才出現的，也是規格版號往上跳的原因。強的 agent 很少用「欄位留空」的方式失敗。它們的失敗是**把欄位填得很有說服力、底下的工作沒做**。一行寫著 `verified_by: 搜過所有呼叫點，沒有其他站點` 的字，不論那次搜尋有沒有跑，在紙上長得一模一樣。下游工作於是建立在一個宣稱上、而不是一個事實上，而它會在最貴的時刻才浮出來。

同期兩個形狀。某個功能合併了、pipeline 回報成功、線上服務卻一直在跑上一個 commit。最自然的解讀是「部署比較慢」。不是：那個 process 正在 crash-loop，因為一個可為空的欄位沒有標明型別、資料層在啟動時推不出來。型別檢查過了、build 過了、只跑依賴注入的 smoke test 也過了；**只有把服務對著一個真的資料庫啟動起來**才重現得出來。第二個形狀：一批回應標頭的安全強化，靠一條綠色的 pipeline 就被標成完成 — 而去問線上服務「你現在跑的是哪個 commit」，答案是舊的那個。

注意這五項的共同點。沒有一項是能力問題。它們全都是**排序問題**：能擋下每一項的那個檢查，本來就存在、也很便宜，只是做得太晚、或根本沒做。

---

## 2. 排序上的修正

拿一個非瑣碎的任務，把兩種順序都寫出來。

**預設的順序。** 描述任務 → agent 生成 → 發現在生成過程中發生 → 重新生成 → 又發現 → 出貨 → 才知道自己拿到了什麼。每一次發現都落在「已經有東西依賴它」之後，所以每一次都同時要付學習成本與拆除成本。而驗證如果有發生的話，發生在最後 — 也就是最貴、而且最容易被省掉的位置。

**MTM 的順序。**

1. **把結果寫成可觀察的句子。** 不是「實作批次重發」，而是「管理員選取這些人之後，每個人各收到一封新邀請」。
2. **列出必須成立的事，並在每一條旁邊寫下你是怎麼查的。** 一次搜尋、一句查詢、一個對線上服務的請求、一行 log。
3. **列出不該由你決定的選擇，然後停下來。** 範圍有歧義、屬於產品判斷、或任何「兩種讀法會做出兩種軟體」的地方。
4. **然後才生成** — 一次，對著一個寫下來的標的。
5. **拿每個聲稱的結果去對實際觀察到的東西**，並誠實標出你還沒觀察到的。
6. **讓一個沒有寫過這段 code 的讀者**對照計畫與 diff，通過了才算完成。

關於這個順序，有兩點比個別步驟更重要。

**那些檢查便宜，是因為它們站的位置。** 一次搜尋、一句 schema 查詢、對人問一個問題 — 單獨看都微不足道。在還沒有任何東西依賴它們之前做，成本幾乎為零，而且經常會改變計畫。**在生成之後做，同樣的檢查就不便宜了**，因為它現在會讓工作作廢。這套紀律不是「多驗一點」，而是「**早一點驗**」，而早驗通常代表總量驗得**更少**。

**那份產物必須是一個檔案。** 第 1–3 步產出的是寫下來、存在對話之外、agent 會去**讀**而不是去**重建**的東西。對話每次被壓縮就要重新推導一次；一份短檔案是讀進來的。這也是這套方法論以 markdown 出貨、而不是以工具出貨的原因 — 見 §6.1。

本文其餘部分就是讓這個順序**實際可行而不只是志向**的機械：一個任務該跑多少、到底要寫下什麼、以及是什麼在阻止 agent 在什麼都沒驗的情況下寫下「已驗證」。

---

## 3. 架構：一條 lifecycle

2.0 把原本三份各自為政的文件收成**一條 pipeline、一份產物**。七個 phase，而多數任務只會真的跑到三、四個。

| # | Phase | 做什麼 | 產出 | 典型成本 | 什麼情況跳過 |
|---|---|---|---|---|---|
| **0** | **分級** | 依**可觀察的觸發**（而非感覺）決定這個任務跑到哪一層（§5） | 一個級別 | 數秒 | 永不跳過 — 但它會把大多數任務路由到「幾乎什麼都不做」 |
| **0-P** | **Plan**（僅綠地） | 還沒有 code：用生活語言把難以回頭的地基選擇問清楚（§8） | 架構種子文件 | 一次對話 | 任何既有專案 |
| **1** | **接地** | 列出這個任務所依賴的架構斷言；每條都要指出支持它的文件或程式碼。沒有依據的斷言要**問**、不要**假設** | 已接地的斷言、或問題 | 數分鐘 | 全部已有文件支持 |
| **2** | **升級決策** | 當請求無法一對一對映到資料模型時，列舉候選解讀；攤開真實選項與取捨；由人選 | 記錄下來的共識 | 數分鐘 | 沒有任何歧義 |
| **3** | **契約** | 寫下那份產物（§4） | 一個檔案 | 最重的級別 15–30 分鐘；最輕的級別三欄 | 完全落在既有決策紀錄內 |
| **4** | **實作** | 在宣告的邊界內把它做出來 | 出貨的 code | 真正的工作量 | — |
| **5** | **自查** | 對每一條標記通過／失敗／變更並附理由，並記下**實際觀察到**什麼 | 補完的契約 | 數分鐘 | — |
| **6** | **驗證** | 由一個乾淨 context 的讀者對照契約、決策與 diff（§9） | 審查報告 | 一輪 | 低風險工作 |

這條 pipeline 有三個性質值得點名，因為它們決定它日常好不好用。

**一份產物，從 phase 1 活到 phase 6。** 同一個檔案隨任務長大，頂端帶一個狀態列：現在哪個階段、卡在什麼上、哪些前提還沒閉合、哪些問題還開著。當 session 被摘要或交棒時，那一列就是唯一要看的地方。這是對浪費 #3 的直接反制。

**每個 phase 都有 fast-path。** 每個 phase 都有一個「幾秒鐘通過」的條件。假設全都已有文件支持的任務，phase 1 立刻過；沒有歧義的任務，phase 2 整個跳過。這條 pipeline 是**設計成大部分會被跳過**的 — 而這只有在「跳過與否由規則決定、而不是由心情決定」時才成立，所以有 §5。

**實作階段有三條硬規則。** 不准用 flag、toggle 或隱藏欄位去繞過契約宣告的邊界。如果實作到一半發現非穿越那個邊界不可，停下來回 phase 2。如果要改宣告範圍之外的東西，先講再動。這三條存在的理由是：**最傷的漂移不是一個糟糕的決定，而是一個好決定後來被安靜地推翻。**

---

## 4. 要寫下什麼

那份產物是一個 markdown 檔。它的欄位不是表格，而是**三條驗證鏈**，加上讓這三條鏈有意義所需的脈絡。

**鏈一 — 前提。** `preconditions`，每條帶 `verified_by`：那個確立它的、可執行的東西。「邀請接受路徑是 idempotent 的 — 依據：在這個 commit 讀過它」。寫下這一行，會強迫一次搜尋、一句查詢，或一句誠實的 `UNKNOWN：我需要查什麼`。幻覺出來的 API 就是死在這裡。

**鏈二 — 結果。** `expected_outcome`，每條帶 `verifiable_by`（將怎麼驗）以及 `observed_result`（實際看到什麼）。後半是 2.0 新增的，而它帶著讓整套方法長出牙齒的那條規則：

> **證據還是一句承諾時，不准把該條標成通過。** 要嘛記下你這個 session 真的跑過什麼 — 指令輸出、查詢結果、log 行、觀察到的值 — 要嘛標成 `UNVERIFIED` 並把它帶進審查當未閉合項。「待驗」是中繼狀態，**不是通過**。

沒有這條規則，一個有能力的 agent 產出的文件，跟真正的嚴謹**無法區分**。有了這條，兩者在紙上長得不一樣。

**鏈三 — 假設。** `schema_assumptions`，每條帶 `source`：關於資料形狀、預設值、不變量的信念從哪來。沒有來源的假設會被標成推測，並優先被檢視。

圍繞這三條鏈：

| 欄位 | 它擋掉什麼 |
|---|---|
| `intent` | 一句可觀察的話。擋掉「我說完成就是完成」。 |
| `affected_layers` | 什麼會改、**以及什麼刻意不改**。擋掉連帶損害。 |
| `cross_module_contract` | 我發出什麼、聽什麼、假設別人做什麼、承諾呼叫方什麼。擋掉靜默的介面破壞。 |
| `confidence` | 總體信心，加上逐條列出的低信心子題。讓不確定性有一個合法的存放位置，agent 於是既不硬掰也不乾等。 |
| `escalation` | agent 不准自己決定的事、以及該讓它停下來的條件。同時擋掉「自信的錯答案」與「沉默的停手」。 |
| `grounding` | 契約自身主張的引用來源。沒來源的主張會被標記，不會被洗白。 |
| `rollback_plan` | code、schema、環境。也強迫思考上線順序（§10.5）。 |
| `test_plan` | 本機、staging、production。搭配 phase 5 讓「它可以了」這句話有意義。 |
| `status` 狀態列 | 階段、卡點、未閉合前提、未決問題。任何 context 遺失後的復原點。 |

在最輕的級別，你寫三欄、不是十二欄。這就帶到決定整套東西是資產還是稅金的那一節。

---

## 5. 路由：什麼任務才吃完整流程

一套**一律適用**的方法論就是稅。路由規則是效率的保證書，而且刻意做成機械式的。

| 級別 | 長相 | 吃到什麼 |
|---|---|---|
| **瑣碎** | typo、文案、版號、單一小檔、純樣式 | 不寫。直接做。 |
| **局部** | 單一模組、且未命中下列任一觸發 | 只有三條承重欄：`intent`、`escalation`／候選集合、`affected_layers` |
| **結構** | 碰到 domain 邊界、跨模組契約、或新的持久化 entity | 完整契約 + 接地 + 升級決策對話 |
| **關鍵** | 權限、安全、schema migration、上架資產、多租戶可見性、金流 | 完整契約 **+ 強制獨立審查** |

**級別由可觀察的觸發決定、不由 agent 的風險感覺決定。** 碰到認證、權限或密鑰嗎？有 schema 變動嗎？跨超過一個 domain 嗎？改動超過 *n* 個檔（*n* 由你的專案自訂）嗎？碰到租戶可見性、付款或上架資產嗎？字面請求無法一對一對映到資料模型嗎？命中任一就升級，而且 **agent 不准把自己講回來**。關鍵級別完全不可自行下降。

這看起來像官僚，實際上恰好相反。這裡唯一不能信任的東西就是自評 — 因為被要求的那個判斷（「這件事夠危險、需要謹慎嗎？」）**正是謹慎存在來 backstop 的那個判斷**。趕進度時它會退化，而那正是最需要它的時刻。一份短的可觀察觸發清單既更便宜、也更難被合理化繞過。

**這張表分不了 bug。** 上面每一條觸發都建立在已知的 scope 上，而 bug 的 scope 正是你還不知道的東西 —— 那才是它之所以是 bug。所以表旁邊有一條繞道，它自己有兩個可觀察的觸發：**已經試修過一輪但沒解**，或**某個值顯示錯了而根因未確立**。任一命中就停止改 code，改寫四件事 —— 症狀；已經猜過什麼、**以及每一次各自的結果**；能夠了結它的前提，每條附上會跑的那個檢查；還有證據要從哪裡來，**在形成假設之前**先決定。影響層級標成未知，直到前提閉合；閉合後再用**已知的** scope 正常分級。

它是一條繞道而不是另一個級別，因為 debug 真正貴的是那個迴圈，而迴圈的燃料是「還沒確立任何事實就先動手」。級別幫不上這個忙，一個「不准繼續」可以。

那條老經驗法則依然成立，而且值得明講：**如果寫契約比寫 code 還久，你不需要契約。** 但要注意它的**適用範圍** — 它只在最輕的兩個級別成立。一個 schema migration 不會因為契約寫起來很久就變成局部級別，規格對此有明文：關鍵級別不可自行下降，結構級別也不能靠這個論證下降。級別表存在的意義，是讓這個判斷由規則做一次，而不是每次趕的時候臨場做。

---

## 6. 為什麼長這樣

五個設計決定承擔了大部分重量。每一個都來自觀察、不是偏好。

### 6.1 是一個檔案，不是一個工具

這套方法論以零依賴的 markdown 出貨，工具刻意排在後面。兩個理由。第一，那份產物的工作是**活過 agent 的 context**：它必須短到可以重讀、好掃到可以從中復原、樸素到任何工具與任何模型都吃得下。檔案做得到；資料庫的一列或一個 IDE 外掛做不到，而一段對話當然更不行。第二，在形狀還沒穩定之前就把方法論鎖進工具，產生的工具債會大於它帶來的採用好處。強制執行是有價值的 — 見 §14 — 但它該在形狀停止移動之後。

### 6.2 升級決策是一等 phase

實務上最高價值的時刻不是契約、不是審查、也不是任何一個欄位。是**動手前那個「agent 攤開這個請求可能是什麼意思、由人來挑」的點**。兩個反覆出現的形狀：

- **字面請求無法一對一對映到資料。** 「批次重發邀請」實際上至少指向四種人：從未邀請、已邀請未接受、已過期、接受過但之後移除。挑一個統計上最可能的解讀的 agent，會交出**正確但瞄錯目標的 code** — 這是最貴的一種錯，因為沒有任何東西壞掉，而每一件事都是錯的。
- **設計本身值得重新考慮。** 有一次初版方案提議把回收到的資料複製進某個人的個人通訊錄；人在實作前讀到那份計畫，直接否決了整個形狀、改成共用的儲存。那次對話花了幾分鐘。同樣的領悟如果發生在實作之後，代價是一次資料搬遷。

所以升級決策不是寫契約時的一條子規則。它是自己的一個 phase、有自己的產出，而且**它是最後一個「改方向還免費」的 phase**。

### 6.3 內部挑戰、有條件外露

早期版本要求 agent 在每個任務上都提出一個反對意見。這條規則被移除了，理由很有啟發性。在所有「挑戰真的產生價值」的任務裡，**沒有一次來自 agent 自己製造出來的反對**。全部來自 agent 攤開真實選項、然後人重新框問題。強制外露會製造噪音；噪音訓練你跳過那一段；然後真的該擋的那一次，也一起被跳過了。

2.0 的規則是不對稱的：**內部**，agent 每次都必須跑一次挑戰 —「如果我要反對這個方向，最強的論點是什麼？它有實質嗎？」**外部**，只有答案是「有」的時候才提出來。這保住了那個能抓出「討好但錯」的檢查，而不必為每個任務付一場表演的錢。

### 6.4 驗證的意思是執行、不是宣告

§1.5 與 §4 已經講過。把它列在設計決定裡，是因為它**推翻了這套方法論第一版的一個假設**。那一版假設失效是「agent 不會去做那件工作」。在有能力的模型上，失效是「agent 沒做那件工作**而且回報說做了**」 — 不是因為不誠實，而是因為**產生一句像樣的驗證句，比執行那個驗證容易太多**。解法不是加更多欄位，而是一條讓「沒執行的檢查」在紙上長得跟「執行過的檢查」不一樣的規則。

### 6.5 最小版必須真的很小

11 欄的版本對高風險工作是合適的，對其他一切都太重。而重有一個很特定的失效模式：當整套東西感覺很貴，它會**整個被跳過** — 而你損失的不是那些「少了也不會怎樣」的中段欄位，是那兩三個承載了幾乎全部價值的欄位。所以最輕的級別只有三欄，選的標準是**承重能力**而不是完整性：這件事完成時什麼會是可觀察地成立、我不准自己決定什麼、我在動什麼。中段欄位有能力的模型本來就會做；那三欄，你不要求它就會跳過。

---

## 7. 一個任務，從頭到尾

以下抽象自一個真實任務。產品脈絡已移除，形狀完整保留。

**請求。** 「加一個使用者語言偏好，讓後端能用使用者的語言送訊息。」一個高頻讀取表上的可為空欄位、一份訊息字串對照表、一個要在進來的請求上讀的標頭。看起來像一個下午的事。

**Phase 0 — 分級。** 有 schema 變動、跨超過一個模組、影響那張使用者表的**每一次讀取**：關鍵級別。完整契約 + 強制獨立審查。注意這裡完全沒有動用判斷 — 光是「有 schema 變動」就足夠了。

**Phase 1 — 接地。** 這件事依賴的斷言，每條都要來源：訊息字串目前產生在一個地方（不是 — 它們產生在很多地方）；推播路徑可以帶語言（可以，但那個點必須被找出來）；某處有權威的預設語言（沒有 — 那是要被決定的）。

**Phase 2 — 升級決策。** 兩個不該由 agent 回答的問題：語言未知的使用者怎麼處理，以及「未知」該對映到產品的原生語言還是國際預設。兩個都由人在一分鐘內決定。記錄下來，因為三個月後還會被問一次。

**Phase 3 — 契約。** 節錄：

```markdown
## status
stage=3 | tier=關鍵 | blocked_on=[] | unverified_preconditions=[]

## intent
偏好設為第二語言的使用者，收到的系統訊息為該語言；
所有既有使用者的行為完全不變。

## affected_layers
entity：+ 一個可為空欄位 ·  service：訊息查表、通知發送
endpoint：讀取請求的語言標頭 ·  migration：有
client：登入時回報自己的語言 ·  管理台：不動

## preconditions
- 那張使用者表的每一次讀取都經過資料層的預設選取
  verified_by：在這個 commit 搜過全部 query builder
- 通知路徑可以帶每位收件人的語言
  verified_by：讀過發送函式簽章，它接受一個收件人物件

## expected_outcome
- 既有使用者看不出任何變化
  verifiable_by：比對改動前後產生的字串，逐 byte 相同
  observed_result：PENDING
- 設了偏好的使用者收到第二語言
  verifiable_by：整合測試同時斷言兩種語言
  observed_result：PENDING

## escalation
由人決定：未知語言的預設；「未知」是原生語言還是國際預設。

## rollback_plan
欄位可為空、且被排除在預設選取之外，所以 code 在 migration 之前或
之後上線都安全。回退就是一次 code revert。
```

**Phase 4 — 實作。** 很順，因為歧義在開始之前就消失了。

**Phase 5 — 自查。** 每條標記；逐 byte 的比對真的跑了、並且是以「觀察」而不是「承諾」的形式被記下；兩個要等部署後才觀察得到的結果，明確留成未驗證。

**Phase 6 — 審查。** 這一步讓這個任務值回它的成本。一個乾淨 context 的讀者，只拿到契約、先前的決策與 diff，提出了實作 context 裡沒有人想過的事：**上線順序**。如果 code 先於 migration 部署，資料層就會開始選取一個還不存在的欄位 — 而因為那是使用者表，**每一次讀取都會失敗。每一次登入。** 型別檢查、build、啟動測試全部會過，因為它們都不查 production 的 schema。

有意思的是**被選中的那個修法**。不是「記得先跑 migration」 — 那是一條依賴人記得的程序 — 而是**把那個欄位排除在預設選取之外，讓危險的順序變成無害**。審查同時要求補一個契約測試把「未知語言」那個決定釘死，讓未來若翻案會**大聲失敗**、而不是安靜地變掉。

**這個流程實際買到了什麼。** 歧義的價格是一分鐘、而不是一次重寫。順序地雷是由一個「不是作者」的讀者在部署前找到的、而不是在一次服務中斷裡找到的。而那個修法從「一條要有人遵守的程序」變成「一個讓錯誤不可能發生的設計」 — 這是這套方法能產出的最好結果的一般形式。

---

## 8. 從零開始

上面那條 pipeline 假設你已經有 code。如果你是從一句話開始 —「我想要一個能做 X 的 App」 — 有一個分支處理這件事，因為失效模式不一樣：**沒有東西可以接地**，而且**最要緊的那些選擇，正是提問的人不知道自己正在做的選擇**。

這個分支有一個開場問題，然後一份短的岔路清單。

**先問目的。** 在任何岔路之前：你做這個最想達成什麼？這是整個流程唯一預設會問的開放問題，而它的答案真的有下游工作 — 它標定了**哪個體驗不准被安靜降級成佔位**，也決定了哪些岔路重要到值得問。如果答案是同義反覆，不要追問；直接落到岔路機制。

**而且要拿這個答案回頭當檢查，不只當方向。** 如果他交代的東西與他剛講的目的**明顯矛盾**，不要照做，也不要自作主張改 —— 把矛盾攤給他：「你說最想達成的是 X，但你要的這個會導向 Y，是我理解錯了，還是這中間有我不知道的考量？」門檻要守在**明顯矛盾**。放寬成「可以更貼合目的」，就會養出一個對每個請求都反問的 agent，而那會教會對方跳過這個問題 —— 然後真正該擋的那次也一起被跳過。輸出是問題、不是拒絕；決定權仍在他。

**然後只問難以回頭的選擇**，而且用**後果的語言**、不用架構的語言。不是「你要不要一個後端資料庫」，而是「換新手機的時候，你的東西要還在，還是重來也沒差？」不是「你需不需要多租戶」，而是「你的合夥人要能登入、而且只看得到他自己那份嗎？」那些之後改很便宜的岔路 — 樣式、命名、列表還是卡片、通知預設 — 直接給一個合理的預設值，收在一個看得見的「我先這樣假設」區塊裡，而不是拿去問。

**四個岔路永遠不給靜默預設**，因為猜錯的代價是重寫、隱私事故或法律問題：資料要不要跨裝置留存；是不是多人登入；誰看得到誰的資料；有沒有碰到錢或別人的個人資料。

**確認的時候用「能力」、不用「結構」。** 不拿 entity 圖給人看 —「第一天你就能做 A、B、C；你還不能做 D、E。」那份**排除清單**才是讓人說出「等等，我合夥人不能登入？」的東西，而那句話你要在實作之前聽到、不是之後。

產出會寫成主 pipeline 讀得懂的架構文件，每個未解決的岔路明確標成 unknown，於是 phase 1 會 fast-path、而不是換成開發者的詞彙再把同樣的問題問一遍。

---

## 9. 那道值回成本的閘

在關鍵級別，有一個 phase 是強制的：一個乾淨 context 的讀者，只拿到契約、架構決策與 diff，檢查計畫與結果是否一致。

不是作者。也不是產生這段 code 的那場對話的延續。理由不是誠信問題，而是：**產生那個缺口的推理，跟會去審它的推理是同一套。** 一個剛花一小時把某個東西蓋出來的人，腦中有一個「它是對的」的運作模型，而從那個模型內部審查，找到的是錯字。

這道閘實際抓到什麼 — 取自一週內連續四批高風險工作，每一批都通過了靜態分析、單元測試與自我審查，而每一批都被判定**不准出貨**：

- 後端做完整了，而**前端的入口從來沒接上**。可運作的 code、到不了的功能。
- 一個版面在 runtime 炸掉。審查者用一個作者從沒寫過的測試重現了它；靜態分析看不到這種東西。
- 「清除截止時間」安靜地什麼都沒做，因為「省略該欄位」與「把它送成 null」對那個端點來說**無法區分**。
- 一個新的通知型別漏在白名單外 — 這是同一個遺漏的**第二次**發生，也因此它變成了一條常設檢查項。

有兩條紀律讓審查有用而不是裝飾。**它只報不修**，所以發現會回到工作的所有者手上，而不是被審查者安靜地補掉。而它最重的判定不是「這壞了」，而是**「這能跑、而且違反了當初講好的設計」** — 因為能跑卻牴觸紀錄下來的決定的 code，是會複利的那種失效。

**誠實的界線：** 這種獨立性是 **context 級、不是組織級**。一個沒有實作記憶的乾淨 agent context，在找作者看不見的缺口上明顯有效。但它不是第二個團隊，也不免疫於它所屬模型家族的盲點。

### findings 是判斷的輸入，不是判決

審查的價值來自視野受限——而同一個限制代表它**看不見意圖**。它拿到的是契約、決策與 diff，沒有拿到「當初為什麼這樣選」的那段對話。

所以報告回來時，握有完整 context 的那一方欠它的是**判斷**，不是服從。每條 finding 問一個問題：**這撞到的是缺陷，還是決定？**

撞到缺陷就修。**撞到決定，不歸你翻** —— 說出當初的理由，把選擇交回做那個決定的人。安靜地把已經決定的事翻掉，是最貴的一種服從，因為它**看起來像嚴謹**，卻丟掉了交辦者唯一還在貢獻的東西。

兩道防護讓它不會變成盾牌。**對稱舉證**：要接受，說得出它會怎麼壞；要拒絕，指得出當初的決定。「審查者說的」不是理由，「我有我的考量」也不是。以及**自我檢驗**：如果你沒辦法不編造地說出理由，那就不是決定、是疏忽 —— 這條 finding 成立。而如果你**說得出**理由，還要再問一次：那個理由當初驗過沒有？**建立在沒查證前提上的決定，同樣算疏忽** —— 否則這條規則會反過來保護正是審查存在要拆穿的那種失效。

處理完，把理由寫到下一位審查者看得到的地方。**一條 finding 撞到「沒被記錄的決定」，本身就是訊號**：那個決定記錄不足。

---

## 10. 七種划得來的情境

反覆出現的形狀，用你實際遇到它的樣子寫。三個寫滿、四個從簡。

### 10.1 螢幕上的值是錯的

某個東西顯示了錯的內容。沒有 crash、沒有 log。誘人的做法是推理資料流，而且**每一輪推理都很合理** — 這正是它能吃掉一整個下午的原因（見 §1.4）。

取代推理的規則：**遇到值錯掉的 bug，第一個動作是列舉出那個值的所有產生點，然後全部讀過。** 不是一個假設，是全部的產生者。兩個用痛換來的推論：**在共用 helper 上加 fallback，不會修好那些從不呼叫它的複製品**；反過來，看到一個帶 fallback 的 helper，就假設 inline 的複製版存在，直到你真的看過。還有一個相關的陷阱：**一個錯掉的值可以假扮成第二個、看起來無關的 bug。**

*誠實註記：這個案例沒有契約。它正是紀律被一輪接一輪跳過、直到有人堅持才去做那次搜尋的那一次。**一套方法論「沒有被遵守」本身就是一個真實的失效模式**，而這就是它的代價。*

### 10.2 全部都編譯過了，型別還是對不上

一個 join 對明顯存在的資料回傳空的。連續三個假設 — 前端快取沒更新、身分不對、資料層推錯欄位型別 — 每個都產生一次改動、一次重建、以及零資訊。答案來自**一句對資料庫自己的欄位目錄的查詢**：join 兩側的型別不同。應用層再怎麼推理都不會浮出來，因為資料庫之上的每一層**內部都是自我一致的**。

對應的欄位是帶 `verified_by` 的 `preconditions`，但那條紀律比欄位更小：**第一輪沒修好，就停止改 code。** 先確立一個事實。連續的猜測不只浪費它們自己的成本，還留下殘渣讓後面的輪次更糟。

### 10.3 請求說的，不是資料的意思

請求只有一句話、而且讀起來毫無歧義。資料模型說它指向四種不同的人。不論 agent 挑哪一個，code 都會是正確的、瞄的是錯的目標，而你會在它已經存在之後才知道。

做法是**把候選集合寫出來、由人挑** — 在實作之前，那時候「列舉」就是全部的成本。這就是 §6.2，而它是整套方法裡單一價值最高的習慣。如果你只想從這篇文章拿走一件事，拿這件。

### 10.4 pipeline 綠燈，而東西沒上線

見 §1.5。**綠色的 build 是一個預測、不是一個觀察。** 便宜的反制：新的持久化 entity，要對著一個真的資料庫啟動過才算完成；任何部署過的東西，去問線上服務它現在跑的是哪個 commit。

### 10.5 改動沒問題，你上線的順序有問題

見 §7。一般形式：只要一個改動同時跨 code 與 schema，「哪個先落地、以及中間那段時間會發生什麼」就是一個**獨立的設計問題**。**寧可讓危險的順序變成無害，也不要寫下正確的順序。**

### 10.6 一個平台好，另一個平台安靜地不好

同一個功能、同一段共用 code、其中一個平台微妙地比較差。有一次是方向偵測：對四個旋轉角度各跑一次辨識再比分數 — 一個平台的辨識器對方向敏感，所以正確的角度會明顯勝出；另一個平台的辨識器**對旋轉免疫**，分數拉不開、勝出者是雜訊。**當底層不同時，共用 code 不是共用行為的證據。** 在 `affected_layers` 裡把平台分開列，並各自觀察。

### 10.7 選擇證據來源，本身就是工作的一部分

一次認證失敗吃掉了一整天。所有教科書式的檢查都做了也都排除了 — 憑證指紋、scope、同意狀態、幽靈憑證、專案歸屬。答案是**裝置自己的 log 裡的一行**，明白寫著某個憑證的型別不對。它一直都在那裡。**在形成假設之前先決定證據會從哪裡來**；五條前提都對著最弱的可得來源驗證，產出的是一份很有自信的錯契約。

---

## 11. 什麼時候該跳過，以及它修不了什麼

**跳過它**：typo、文案與樣式、版號、你已經確立根因的一行修正、以及不會產生 commit 的探索性工作。判準不變、適用範圍也不變：**寫契約比寫 code 久，就不需要契約** — 只限最輕的兩個級別（§5）。

**它沒能修好的東西**，來自產出上述一切的同一段期間：

- **第一版契約常常過度設計。** 不只一份初版契約提出了比問題所需更重的設計，然後在審查或升級決策階段被砍下來。**價值在迭代、第一版不是聖經**。要預期自己會寫一份更簡單的 v2。
- **慎重會延遲出貨。** 有一個臨時替代方案在 production 活了好幾週，等正式設計收斂。這是真實成本，而「我們當時很謹慎」不會退款。
- **大型工程的範圍仍然被低估。** 一次多語系工程被當成前端任務估算，結果需要大量後端工作。契約讓假設顯性化；它不會讓你變得擅長估算。
- **隱藏依賴照樣會漏過。** 一份寫得很完整的契約漏掉了一個在畫面掛載時發出的呼叫，上線後才浮出來。這個教訓變成了**那個 codebase 自己契約裡的一條常設檢查** — 畫出掛載時的依賴圖。注意**沒有**發生的事：它並沒有變成規格裡的規則。單一事件是資料、不是規則；§12 說明為什麼那還不夠。
- **量測工具自己會漂移。** 我們自己的紀錄至今已經退化過**三次**（§12）。如果一套方法論連自己的紀錄都維持不了，那就該懷疑它任何依賴完整紀錄的主張。我們的主張確實依賴，而 §13 明講了這件事。

---

## 12. 它會改自己

2.0 不是我們一開始的那份規格，而**改變它的那套機制本身就是設計的一部分**。

四個步驟。每個非瑣碎任務結束時，往一份案件記錄追加一行：第一次就過了嗎、有沒有幻覺事件、哪個欄位抓到或漏掉了什麼、升級決策值多少。當一個模式反覆出現、或審查者指出的是**規格本身**（而不是 code）的缺口，它就成為一個提案。**提案不會自己生效** — 由人決定。通過就是改規格、記變更、動版號。

做最多工的是提案必須通過的那**兩道測試**：

1. **實質** — 真的是改善，不是措辭調整。
2. **跨任務通用** — 對未來各類工作都成立，不是為了補**這一次單一事件**的洞。

第二道才是重要的那道。沒有它，機會主義式的改善會退化成**逐事件打補丁**，而規格會長成一堆「每條規則在局部都成立、組合起來卻不連貫」的東西 — 這正是這套方法論存在要防的失效，套用在它自己身上。**單一事件進案件記錄當資料點；只有當它能被論證為通用、或它復發了，才變成規則。**

關於這具引擎實際上怎麼運作，有兩個誠實的觀察。

**它是爆發式的，不是穩定的。** 有四個版本在**同一天**通過 — 那天是三份獨立評審被委託出去的那天。案例累積了好幾週都沒長出提案，然後一次集中的批判一口氣產出好幾個。**排定的對抗性審查，對一套方法論的作用，明顯大於被動累積。**

**一條為了修「紀律衰退」而被升級的規則，自己也衰退了。** 案件記錄原本是軟性的：「任務完成時追加一行。」它停止發生了。所以它被升級成硬性關卡 — **沒有那一行就不算完成** — 論證是「軟紀律必然開天窗」。一個月內，那道關卡就被忽略了：連續十個符合條件的任務出貨，沒有任何一行記錄。而那條規則**全程都在規格裡**。

結論不太舒服，但值得公開：**一道沒有機械執行點的「硬」關卡，本質上仍然是軟紀律。** 在文件裡把一條規則稱為強制，並不會讓它變成強制。這是 §14 那些強制執行工具最強的論據 — 而這個論據是這套方法**失效**產生的，不是它成功產生的。

---

## 13. 證據，以及它的界限

**這一切建立在什麼上。** 11 週約一百份動手前契約，在一個商業 codebase 上 — 行動端 App、後端服務、兩個 web 管理台 — 實作大部分由 AI agent 完成。外加一次在另一個綠地專案上的對照實驗。

**那次對照實驗，包括我們輸掉的部分。** 同一句話的產品請求交給兩個獨立的建置，一個用這套方法、一個不用，然後由一個**中立 agent**（同時拿到兩邊成果與原始那句話）評判。方法在**最難回頭的決定**上贏了：它選了原生 App 而不是網頁版，而提出請求的人確認那正是他在意的決定；它也把主要互動模型選對了。它**輸掉**的地方更難看：請求的人在那句話裡**明確點名了一個核心體驗**，而用了方法的那一版把它做成佔位，沒用方法的那一版當場接上了一個真的實作。**架構是對的，而那個人來就是為了的東西沒有被交付。** 這個結果產生了一條新規則 — 使用者字面點名的核心體驗是一等交付物、不准被降級成佔位 — 它現在是規格的一部分。

**我們沒有量的東西。** token。本文不宣稱任何關於 token 花費的數字或倍數，因為我們從來沒有測量它。被記錄下來的是重試、重建、以及收斂前的輪數 — 這些是**代理指標**，讀者應該把 §1 的論證當成「關於成本結構的推理」，而不是當成量測結果。

**不可外推的東西。** 最初十二個任務有完整量測：十二個全部第一次就落地、零幻覺事件。那些數字是真的，而它們**不延伸到其餘的任務** — 其餘任務並未以同樣方式計數。後期記錄到兩次幻覺事件，都是關於第三方 SDK 介面的斷言、在鎖定版本上並不存在。兩次都是**在任務進行中、有人真的去跑那個檢查的那一刻**被抓到，不是在 production — 而且其中一次是**審查階段自己產生的**，這本身就是一個關於「驗證必須咬在哪裡」的教訓。**更嚴格的量測套在更寬的介面上，會產生更難看的數字**，而那正是這個修正的誠實方向。

**其他界限。** 單一 codebase，所以領域效應無法與方法效應分離。全程同一組人與 AI 的組合，所以審查的獨立性是 context 級而非組織級（§9）。沒有 benchmark 套組：不存在一組可重現、藏有陷阱的任務集，讓別人拿它對照量測這套方法。這仍然是我們無法獨力完成、卻最有價值的一件事。

---

## 14. 今天就開始

**可運作的最小版。** 拿你下一個不是 typo 的任務。在 agent 寫任何東西之前，寫三件事：這件事完成時，什麼會是可觀察地成立；什麼不准你自己決定；你在動什麼、以及刻意不動什麼。然後加一條規則到你的工作方式裡：**沒有任何東西可以靠一句承諾被標成已驗證。** 檢查沒跑，就寫沒跑。

這就是全部的最小版。它只有幾行，而依我們的經驗，它抓住了整套方法大部分的價值。完整寫出來（含觸發清單與一份完整的輕量級範例）在 [`MTM-LITE.md`](./MTM-LITE.md) — 一頁，而且是你唯一需要的起手檔案。

**把它包進你現有的工具。** 規格是一個 markdown 檔，所以交付機制就是你的環境會讀的任何東西：給 agent 的行為指示、編輯器自動載入的規則檔、或每個專案貼一次的起手 prompt。同一個核心、不同的信封。沒有東西要安裝。

針對 Claude Code 另有一份打包版本 —— `/plugin marketplace add jewanchen/mtm-contract` 之後 `/plugin install mtm@mtm-contract`，或者想先讀過再說就複製一個資料夾。上述行為就成為預設，並夾帶下一段講的那支驗證器。見 [`plugins/mtm/`](./plugins/mtm/)。

**強制執行。** 這裡有些部分是機械可檢查的 — 沒有空欄位、沒有「證據還是承諾卻標成通過」的條目、沒有「未記錄就結案」的任務 — 而 §12 就是「檢查它們比記載它們更重要」的論據。那些工具刻意放在規格的下游、而不是跟它綁在一起：它屬於持續整合與進階設定，不屬於第一次嘗試這套方法的人的路徑上。

**如果你想認真檢驗它。** 最有用的貢獻不是採用，而是**一次我們自己跑不了的複現**：同樣的任務、不同的 codebase、有紀律與沒紀律各一組、由沒有寫過這套方法的人評判。

---

## 15. 授權、引用、聯絡

本作品採 **Apache License 2.0** 授權。完整條文見 [`LICENSE`](./LICENSE)、署名要求見 [`NOTICE`](./NOTICE)：可自由用於商業與非商業用途、可修改、可再散布，衍生作品需署名 皓品智慧有限公司（Vast Intelligence Limited） — 包含實作本規格的工具、程式庫與改編文件。

```
Vast Intelligence Limited. (2026).
MTM Contract 2.0: An Ordering Discipline for Building Software Products
with AI Agents.
https://github.com/jewanchen/mtm-contract
Published: July 30, 2026.
```

**皓品智慧有限公司 Vast Intelligence Limited**
Email：jeremy.chen@vastitw.com
網站：https://nicemeetapp.com/mtm/

複現研究、整合合作、或強制執行工具的協作，請由上述管道聯絡。

---

*規格參考：[`MTM-CORE.zh-TW.md`](./MTM-CORE.zh-TW.md)（lifecycle、觸發、欄位；英文版 [`MTM-CORE.md`](./MTM-CORE.md)）· [`MTM-Plan.md`](./MTM-Plan.md)（綠地分支）· [`MTM-Verify.md`](./MTM-Verify.md)（審查協定）· [`EVOLUTION.md`](./EVOLUTION.md)（這份規格如何改變）*
